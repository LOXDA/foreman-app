---

# RUBY/RAILS doesn't benefit from hugepages
- name: Disable thp
  ansible.builtin.shell: |
      echo never > /sys/kernel/mm/transparent_hugepage/enabled
      echo never > /sys/kernel/mm/transparent_hugepage/defrag
      echo 0 > /proc/sys/vm/compaction_proactiveness

- name: Install CA Certificates
  ansible.builtin.apt:
    name: ca-certificates

- name: Creating certs file in /usr/local/share/ca-certificates
  ansible.builtin.copy:
    dest: "{{ item.name }}"
    content: "{{ item.filecontent }}"
    owner: root
    group: root
    mode: 0644
  loop: "{{ vault_ca_certs_files }}"

- name: Update ca-certificates
  ansible.builtin.shell: |
    update-ca-certificates

- name: Install gpg
  apt:
    name: gpg

- name: Install Puppet Repository
  block:
    - name: Add Public Key
      ansible.builtin.apt_key:
        url: "{{ puppet_repo_key}}"
        state: present
    - name: Install Puppet Repo
      ansible.builtin.apt_repository:
        repo: "{{ puppet_repo_url }}"
        state: present
        filename: '{{ puppet_version }}'

- name: Install Foreman Repository
  block:
    - name: Add Public Key
      ansible.builtin.apt_key:
        url: "{{ foreman_repo_key }}"
        state: present
    - name: Install Foreman Repo
      ansible.builtin.apt_repository:
        repo: "{{ foreman_repo_url }}"
        state: present
        filename: 'foreman'
    - name: Install Foreman Plugin Repo
      ansible.builtin.apt_repository:
        repo: 
        state: present
        filename: 'foreman_plugins'

- name: Download Foreman installer package
  ansible.builtin.apt:
    name: foreman-installer
    state: latest
    update_cache: yes

- name: Download python3-requests package
  ansible.builtin.apt:
    name: python3-requests

- name: Install ruby-foreman-maintain
  ansible.builtin.apt: 
    name: ruby-foreman-maintain
  when: (ansible_distribution == 'Debian' and ansible_distribution_version >= '11') and foreman_version != '3.3'

- name: "Install postgresql-client-{{ postgresql_version }}"
  ansible.builtin.apt: 
    name: "postgresql-client-{{ postgresql_version }}"

- name: Install Puppet Agent
  ansible.builtin.apt: 
    name: puppet-agent 

- name: Bootstap Puppet Agent ssl
  ansible.builtin.shell: |
    /opt/puppetlabs/bin/puppet config set server {{ groups['tfm_puppet'][0] }} --section main
    /opt/puppetlabs/bin/puppet ssl bootstrap --server {{ groups['tfm_puppet'][0] }}

- name: Ensure group puppet exists
  ansible.builtin.group:
    name: puppet
    state: present

- name: Install and Configure Foreman with Puppet
  block:

    - name: Pre-Install set ulimit
      ansible.builtin.shell: |
        sysctl -w fs.file-max=1048576

    - name: Install Foreman with Puppet
      vars:
        argv_start:
          - foreman-installer
          - --no-enable-foreman-proxy
          - --puppet-server=false
          - --enable-foreman
          - --enable-puppet
          - --puppet-group=root
          - --foreman-db-manage={{ foreman_db_manage }}
          - --foreman-db-database={{ foreman_db_database }}
          - --foreman-db-host={{ groups['tfm_db'][0] }}
          - --foreman-db-username={{ foreman_db_username }}
          - --foreman-db-password={{ foreman_db_password }}
          - --foreman-initial-admin-username={{ foreman_admin_username }}
          - --foreman-initial-admin-password={{ foreman_admin_password }}
          - --foreman-initial-organization={{ initial_organization }}
          - --foreman-initial-location={{ initial_location }}
      ansible.builtin.shell: |
        ulimit -Hn 1048576 ; \
        ulimit -Sn 1048576 ; \
        FOREMAN_APIPIE_LANGS=en \
        {{ argv_start | join(' ') }} \
        {{ foreman_cmd_options | join(' ') }}
      args:
        executable: /bin/bash

    - name: Post-Install with external database (1/2)
      ansible.builtin.shell: |
        FOREMAN_RAILS_CACHE_STORE_URLS='redis://127.0.0.1:6379/4' \
        foreman-rake db:migrate

    - name: Post-Install with external database (2/2)
      ansible.builtin.shell: |
        FOREMAN_RAILS_CACHE_STORE_URLS='redis://127.0.0.1:6379/4' \
        foreman-rake db:seed

    - name: Post-Install with external database (3/3)
      ansible.builtin.shell: |
        ulimit -Hn 1048576 ; \
        ulimit -Sn 1048576 ; \
        FOREMAN_RAILS_CACHE_STORE_URLS='redis://127.0.0.1:6379/4' \
        FOREMAN_APIPIE_LANGS=en \
        foreman-rake apipie:cache:index

# foreman-rake consumme a lot of memory, stop services before using it
# - name: Stop service foreman
#   ansible.builtin.service:
#     name: foreman
#     state: stopped

- name: "Set global Setting"
  ansible.builtin.shell: |
    foreman-rake -- config -k 'trusted_hosts' -v '["{{ groups['tfm_puppet'][0] }}"]'
    foreman-rake -- config -k 'use_shortname_for_vms' -v 'true'
    foreman-rake -- config -k 'destroy_vm_on_host_delete' -v 'true'
    foreman-rake -- config -k 'rss_enable' -v 'false'
    foreman-rake -- config -k 'entries_per_page' -v '50'
    foreman-rake -- config -k 'new_hosts_page' -v 'false'
    # foreman-rake -- config -k 'instance_title' -v 'OPN'
    # foreman-rake -- config -k 'instance_color' -v '#00FF00'
    foreman-rake -- config -k 'login_text' -v 'Instance OPN - Version $VERSION'

- name: Start service foreman
  ansible.builtin.service:
    name: foreman
    state: started

- name: Bootstap Puppet Agent ssl
  ansible.builtin.shell: |
    /opt/puppetlabs/bin/puppet config set server {{ groups['tfm_puppet'][0] }} --section main

- name: Restart service puppet
  ansible.builtin.service:
    name: puppet
    state: restarted

# git clone https://github.com/jdennis/keycloak-httpd-client-install.git
# apt-get install python3-venv
# python3 -m venv keycloak-httpd-client
# cd keycloak-httpd-client-install/
# ../keycloak-httpd-client/bin/pip install .
# ../keycloak-httpd-client/bin/pip install jinja2 six oauthlib requests requests_oauthlib lxml
# ../keycloak-httpd-client/bin/keycloak-httpd-client-install

# starting keycloak >=18 you have to fix the endpoint url in keycloak-httpd-client-install/keycloak_httpd_client/keycloak_rest.py
# by removing the /auth url prefix, or adapting to subpath settings

# export REQUESTS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt
# export KEYCLOAK_URL=https://keycloak.example.com
# export KEYCLOAK_REALM=ssl-realm
# export KEYCLOAK_USER=admin
# ../keycloak-httpd-client/bin/keycloak-httpd-client-install --app-name foreman-openidc \
# --keycloak-server-url $KEYCLOAK_URL \
# --keycloak-admin-username $KEYCLOAK_USER \
# --keycloak-realm $KEYCLOAK_REALM \
# --keycloak-admin-realm master \
# --keycloak-auth-role root-admin \
# -t openidc -l /users/extlogin -d

# foreman-installer --foreman-keycloak true \
# --foreman-keycloak-app-name "foreman-openidc" \
# --foreman-keycloak-realm $KEYCLOAK_REALM

# apache proxy vhost
# with a twist to support both keycloak v<17 and v>17
# <IfModule mod_ssl.c>
# <VirtualHost *:443>
#         ServerName sso.opn.lab
#         ServerAdmin root@localhost
#         DocumentRoot /var/www/html
#         HostnameLookups Off
#         UseCanonicalName Off
#         SSLEngine on
#         SSLProxyEngine On
#         ProxyRequests Off
#         ProxyPreserveHost On
#         RequestHeader set X-Forwarded-Proto "https"
#         RequestHeader set X-Forwarded-Port "443"
#         RewriteEngine On
#         RewriteRule "^/auth/(.+)" "https://sso.opn.lab/$1" [R,L]
#         ProxyPass / https://sso.opn.lab:8443/
#         ProxyPassReverse / https://sso.opn.lab:8443/
#         ErrorLog /var/log/apache2/errors_sso.opn.lab.log
#         LogLevel warn
#         CustomLog /var/log/apache2/access_sso.opn.lab.log combined
#         ServerSignature Off
# SSLCertificateFile /var/lib/puppet/ssl/certs/sso.opn.lab.pem
# SSLCertificateKeyFile /var/lib/puppet/ssl/private_keys/sso.opn.lab.pem
# </VirtualHost>
# </IfModule>

# fix ruby Resquest not using system ca bundle, not great, not terrible
# sed -i 's/:verify_ssl => true/:verify_ssl => false/' /usr/share/foreman/app/services/oidc_jwt_validate.rb